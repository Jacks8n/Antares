#pragma kernel Blit

#include "HLSLSupport.cginc"

uint3 Offset;

Texture3D<float> BlitSrc;
RWTexture3D<float> BlitDst;

[numthreads(8, 8, 1)]
void Blit(uint3 id : SV_DispatchThreadID)
{
    BlitDst[id + Offset] = BlitSrc.Load(int4(id, 0));
}
